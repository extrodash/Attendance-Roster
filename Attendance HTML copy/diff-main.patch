--- src/main.js
+++ src/main.js
@@ -205,7 +205,20 @@
     }
     const total = tracked + blank;
     const pct = total ? Math.round((tracked / total) * 100) : 0;
-    takeTrackingStatsEl.innerHTML = `<small>Tracking coverage since first log:</small> <strong>${tracked}</strong> tracked • <strong>${blank}</strong> blank • <strong>${pct}%</strong>`;
+    takeTrackingStatsEl.setAttribute('aria-label', 'Tracking coverage since first log');
+    takeTrackingStatsEl.innerHTML = `
+      <div class="navigator-stat">
+        <span class="navigator-stat__label">tracked</span>
+        <strong class="navigator-stat__value">${tracked}</strong>
+      </div>
+      <div class="navigator-stat">
+        <span class="navigator-stat__label">blank</span>
+        <strong class="navigator-stat__value">${blank}</strong>
+      </div>
+      <div class="navigator-stat">
+        <span class="navigator-stat__label">coverage</span>
+        <strong class="navigator-stat__value">${pct}%</strong>
+      </div>`;
     takeTrackingStatsEl.hidden = false;
 
     // Update blank navigation list and meta