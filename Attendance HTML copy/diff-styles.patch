--- styles.css
+++ styles.css
@@ -414,27 +414,10 @@
 
 .take-layout {
   display: grid;
-  grid-template-columns: minmax(240px, 320px) minmax(0, 1fr);
+  grid-template-columns: 336px minmax(0, 1fr);
   gap: var(--space-4);
   align-items: start;
 }
-.take-controls { position: sticky; top: var(--header-offset); display: grid; gap: var(--space-4); }
-.control-footnotes { display: grid; gap: var(--space-2); }
-.info-bar {
-  display: flex;
-  align-items: center;
-  justify-content: space-between;
-  gap: var(--space-2);
-  background: var(--color-surface-alt);
-  border: 1px solid var(--color-border);
-  border-radius: var(--radius-md);
-  padding: var(--space-2) var(--space-3);
-  font-size: var(--fs-sm);
-  color: var(--color-muted);
-  border-left: 3px solid var(--panel-accent);
-}
-.info-actions { display: flex; gap: var(--space-2); flex-wrap: wrap; }
-.info-meta { opacity: 0.75; }
 
 .take-summary {
   border: 1px solid var(--color-border);
@@ -736,7 +719,6 @@
 
 @media (max-width: 1080px) {
   .take-layout { grid-template-columns: 1fr; }
-  .take-controls { position: static; }
   .view-panel { padding: var(--space-4); }
 }
 
@@ -751,7 +733,7 @@
 
 @media print {
   body { background: #fff; color: #000; }
-  .app-header, .view-actions, .take-controls, .control-footnotes, .view-support, .modal, #toast, .app-nav { display: none !important; }
+  .app-header, .view-actions, .take-sidebar, .sidebar-footnotes, .view-support, .modal, #toast, .app-nav { display: none !important; }
   main { padding: 0; }
   .view-panel { box-shadow: none; border: none; background: #fff; }
   .people-list { grid-template-columns: repeat(2, minmax(0, 1fr)); gap: var(--space-2); }